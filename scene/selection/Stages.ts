module Snake { 
    export class Stages extends Phaser.Scene {

        container: any;

        constructor(handle: any) {
            super(handle);
        }

        preload() {

        }

        create() {

            this.container = this.add.container(0, 0, )
        }
    }
}